# Requests

T: 难度分级而非重要性分级。

## T0
文案：f0估计器->f0估计算法



## T1


## T2


# Resolved
bug：切分前的 a 引用音频文件的 [0,6)，合成结果覆盖 [0,6)。把 a 切分成 a1、a2 之后，把 a1 拉长到 [0,7)，超出了 a 原本的 6 秒范围。a1 手里的合成音频仍然只有 [0,6) 这一段，多出来的 [6,7) 没有任何合成结果，属于未合成区域。界面上应该出现「超出合成区域」的提示。实际现象：a1 拉出去之后，界面什么提示都没有，[6,7) 那一段看起来跟有合成结果的区域一样。]


- 右下角的坐标要是定宽的，随着坐标的变化，宽度不要变化。不是等宽字体就够了的。
- 右下角的“参数已修改”是什么含义？有什么作用？“完成-耗时xx秒”呢？我认为整个显示推理过程的字都要重新设计一下。
    - 需要一个地方显示推理的过程，但是要在统一的位置；
    - 需要一个地方可以查看当前播放的音频：是否是合成的（还是空的），以及如果是合成的，是用哪些参数（全部展示而不是部分）推理出来的；
    - 添加一个"清空"功能，清空当前副本（A/B）的合成音频（返回为
    - 等等...需要补充的
    - 然后需要一个cohesive且优美的UI设计。
    这些元素现在应该有一部分已经有了，但是似乎是散落各处的；另一部分则还没有实现。统一起来并且设计一个美观的UI。先设计，并且讲述你的设计，再实现。


先帮我纠正一下术语：有的片段被剪切之后（走带中只引用的片段），由于这个剪切的部分是小于原音频文件的长度的，所以头尾可以拉长。
现在的片段如果是已合成的音频，被拉长了之后，超过合成部分就是静音了
这实际上也是合理的，因为超过拉长部分是没有被合成，但是我希望以一种更加能让用户感知到的方式在UI上告诉用户，当前应用片段超出了合成的区域，用户需要重新点击合成音频。
你认为这个要怎么设计？设计一下其中的UI过程，先不要实现


- 我理解A/B/bypass应该是对每个片段控制的而不是对每个音频文件，或者所有实例？但是现在我在一个片段点bypass，结果两个片段都bypass了，很奇怪。
你可以去找我的studio one工程最新改过的那个看一下。理解一下我想表达的是什么，然后告诉我成熟的插件一般都怎么做，先不要改代码，先设计一下
- 给yingmusic-svc推理添加音高微调，-100cents ~ +100cents，滑动条，作为在Pitch shift基础上的delta
    - 输入yingmusic-svc的f0要相应变化
    - 输入PC-NSF-HiFiGAN的f0也要相应变化。
- 音色库点开应该是打开finder到timbre目录下，而不是上一级目录下
- drag-drop到音色库的时候要有一点动画，也不是动画，就是drop有东西的时候颜色边框变化等

关闭daw再打开之后，背后的波形会消失不见。
---
cmd+滚轮，或者触控板cmd+上下滑动：上下缩放（钢琴键宽度缩放）
cmd+shift+滚轮是左右缩放（时间轴缩放）
滚轮是上下滚动
shift+滚轮是左右滚动
这样和studio one的操作逻辑一致；
所有字改成深色。现在还有很多字比如diffusion step, pitch shift, cfg, input gain是白色
输出vocoder里面的字要有一点padding，现在顶着左边。

---
点击合成之后要自动先执行音高检测。

---
实现A/B以及Bypass。你一定需要重新彻底设计一套状态管理，A/B之间所有数据都是不互通的，不要想着共用任何数据等；比如音高A/B应当被分别检测两次，bypass也是A/B分别设置。都需要正确持久化。另外应当在插件合适的地方显示当前播放的是原音频，还是用什么参数合成的音频。另外钢琴卷上出现的音高永远都是原音频的音高。

---
“在Studio One点击右下角的浏览器，点击打开再关闭之后，插件的编辑器可以恢复完整，但是再点击另一个音频片段之后，插件的编辑器又变成截断一一部分的，只有左上角的大部分，右边和下面有黑色的条好像被什么东西遮住了。然后再点击浏览器的打开再关闭之后又好了，但是再点击一个其他音频片段，或者点击其他音频片段再点回来，又会变得被挡住一样了”
之前这个问题没有完全修好，现在新开daw的时候，有时候会有右边有一条竖着的黑色的情况。
彻底研究这到底是什么导致的。然后彻底修好。
---

- yingmusic-svc-mlx增加功能而不是改，因为其还要被audiokit等其他app所使用；
- 需要实现一个build.py脚本，在构建的时候你需要运行的是`python build.py` (不允许带任何shell变量、环境变量、cd、参数等)，然后DAW就应该可以找到安装好的插件。

将更新后的plan写入 docs/ara.md文件。不是让你进入plan mode。

---

timbre library需要有一个打开文件夹(finder)的按钮，点击打开timbre library的finder；另外需要和文件夹里的内容双向同步。

---



请问“保留第一级声码器输出”你保留在哪里？有什么地方给你保留？
改成**输出声码器**选项：
- pupu-vocoder (level 1)
- pc-nsf-hifigan (level 2)
默认是level 2, (level x)要保留。

---

点击开始合成,点击音高检测等东西之后，也应该有对应的进度条和状态显示.
现在的钢琴卷使用触控板移动的速度是错的，鼠标的速度我没有测，可能也是错的，速度太慢了，你是不是*了某种系数
另外，保留第一级Vocoder输出，你保存在哪里了？有什么地方可以让你保存吗?这里应该做成一个选项,改成**输出声码器**选项：
- pupu-vocoder (level 1)
- pc-nsf-hifigan (level 2)
默认是level 2, (level x)要保留。

---

在Studio One点击右下角的浏览器，点击打开再关闭之后，插件的编辑器可以恢复完整，但是再点击另一个音频片段之后，插件的编辑器又变成截断一一部分的，只有左上角的大部分，右边和下面有黑色的条好像被什么东西遮住了。然后再点击浏览器的打开再关闭之后又好了，但是再点击一个其他音频片段，或者点击其他音频片段再点回来，又会变得被挡住一样了
先用日志复现这个问题，然后再修复

DAW关闭之后，所有后台进程应当要停止or杀死，这里需要一个机制，确保DAW推出之后不会还在后台；并且重新启动之后不会出现启动失败等各种问题。先设计一下？

字应该为深色。现在有些地方字是白色，看不见，重新设计该部分

---